return {
  "akinsho/bufferline.nvim",
  dependencies = { "nvim-tree/nvim-web-devicons" },
  version = "*",
  opts = {
    options = {
      -- mode = "tabs",  -- buffers和nvim-tree好像有冲突，会报一下错，但是不影响使用
      -- separator_style = "slant",
      -- hover = {
      --   enabled = true,
      --   delay = 200,
      --   reveal = {'close'}
      -- }
    },
  },
}
